{
  "name": "generated-vitest-project",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:tests": "node scripts/generate-tests.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vite": "^7.0.0",
    "vitest": "^3.2.0"
  }
}
